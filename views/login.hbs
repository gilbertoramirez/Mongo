<form id="login" method="post">
    <input id="email" type="email" name="email" value="" placeholder="Tu Email">
    <br><br>
    <input id="password" type="password" name="password" value="" placeholder="Tu Contraseña">
    <br><br>
    <input type="submit" value="Login">
</form>

<script type="text/javascript">
    const formLogin = document.querySelector('#login')

    formLogin.addEventListener('submit', event =>{
    event.preventDefault()
    const emailStr = document.getElementById('email').value
    const passwordStr = document.getElementById('password').value
    const params = new URLSearchParams()
    params.set('email',emailStr)
    params.set('password',passwordStr)

    fetch('/api/signin', {
      method: 'POST',
      body: params
    })
    .then(res => res.json())
    .then(data => {
        localStorage.setItem('token', data.token)
    })
    .catch(res =>{ alert('Ocurrio un error: '+response.message)})
  })

</script>